# Base URL of the deployed backend API (Render), including the /api path
# e.g. https://collabspace-api.onrender.com/api
VITE_API_URL=http://localhost:5000/api

# Base URL of the deployed backend for Socket.IO (Render), no /api path
# e.g. https://collabspace-api.onrender.com
VITE_SOCKET_URL=http://localhost:5000